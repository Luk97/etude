#pragma once

namespace etude {

    /// @brief A color with red, green, blue and alpha, each from 0 to 1.
    struct Color {
        float r = 0.0f;
        float g = 0.0f;
        float b = 0.0f;
        float a = 1.0f;
    };
}
